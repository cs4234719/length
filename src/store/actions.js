export default {
    // 上下文 context ≈ store
    // Aupdatename(context, payload) {
    //     setTimeout(() => {
    //         context.commit("updatename");
    //         console.log(payload.name);
    //         payload.success();
    //     }, 1000);
    // }
}